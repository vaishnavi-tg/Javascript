// indexOf
function findIndexOf(str,target){
    console.log("Original String is",str)
    console.log("Index of the word is ",str.indexOf(target))
}
findIndexOf("hi hellow and welcome back to my utbe channel","and")

