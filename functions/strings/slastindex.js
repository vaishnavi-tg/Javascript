// LastIndexOf 

function findIndexOf(str,target){
    console.log("Original String is",str)
    console.log("Index of the word is ",str.lastIndexOf(target))
}
findIndexOf("hi hellow and welcome back bacck back back to my utbe channel","back")
